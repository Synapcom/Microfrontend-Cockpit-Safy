import React from 'react';
import Div from './style';

const TransactionsFilters: React.FC = ({ children }) => {

    return(
        <Div>
            {children}
        </Div>
    );
};

export default TransactionsFilters;
